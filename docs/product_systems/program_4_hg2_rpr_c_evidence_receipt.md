# Human Gate 2 Repair Batch HG2-RPR-C Evidence Receipt

## Status

- Status: `AUTOMATED DEVELOPMENT GATE PASSED; MANUAL COMMIT/PUSH CHECKPOINT REQUIRED`
- Date: `2026-08-13`
- Starting commit: `d292e23604b28edbdfc6d8c4f3e16c6ec0c6fb0f`
- Branch: `codex/foundation-audit`
- Scope: `HG2-RPR-C real Focus mode and local-facts Companion doorway only`
- Protected evidence: `NOT USED`
- Git authority: `Jason alone stages, commits, and pushes`

## Delivered Boundary

This batch makes the ordinary Writing Studio quieter without changing the
qualified manuscript, project, planning, critique, or provider boundaries:

- the Companion is one persistent, labelled bottom bar rather than a control
  hidden inside the Writing-session rail;
- its scope is visible before submission: the active local project and current
  writing only, with no AI call;
- a supported local-orientation request immediately opens Command Center with
  the answer, source scope, limitation, and a direct return-to-writing action;
- an unsupported request opens the same bounded result surface with an
  immediate explanation rather than a blank Command Center or implied chat;
- the temporary Companion result is not persisted and neither route changes
  manuscript prose, accepted order, project truth, planning data, credentials,
  provider state, or durable memory;
- Focus removes surface switching, theme controls, rails, edge controls,
  Companion, editor framing, shortcut copy, and line-number gutters; it leaves
  only prose, a quiet project/current-section marker, save state, and an
  explicit exit control; and
- the Companion bar reflows into normal document flow at narrow or high-zoom
  presentation sizes, while the bottom session control moves above the bar so
  no interactive control is covered.

The local Companion remains deterministic orientation only. It is not a local
LLM, generic chat, provider route, durable conversation store, or writing
recommendation engine.

## Automated Evidence

| Check | Result |
| --- | --- |
| Primary Writing Studio component and boundary suite | Green: 99 passed |
| Full application TypeScript boundary | Green |
| First-party and active Stage 19 lint | Green with zero warnings |
| Production renderer and main-process build | Green |
| Built-Electron Companion temporary/reopen and unsupported-request journey | Green |
| Built-Electron large-text, reduced-motion, keyboard, real-Focus, and critique-entry journey | Green |
| Built-Electron Writing shell and continuous-manuscript journey | Green |
| Complete fixed Stage 19 development regression | Green under the documented dirty-development override; no protected evidence used |
| Diff whitespace check before documentation reconciliation | Green |

The first expanded regression run found two layout defects in the new bar:
the Writing Studio root did not follow the light canvas token, and the fixed
bar could cover a control at large text sizes. The implementation was repaired
at the semantic layout layer, then the relevant built-Electron tests and the
complete regression ladder passed.

## Explicit Non-Claims

This receipt does not claim:

- local-model installation, discovery, inference, routing, cancellation, or
  package evidence;
- useful open-ended Companion conversation or writing advice;
- provider use, credentials, remote transmission, paid routing, background
  work, or durable Companion memory;
- full Program 5 import/discovery, automatic chapter acceptance, or Human Gate
  3 completion;
- package/install or hosted exact-candidate qualification; or
- Human Gate 2 author acceptance.

## Next Exact Step

Jason manually stages, commits, and pushes this complete `HG2-RPR-C` batch.
Then `HG2-RPR-D` qualifies the complete repaired candidate, including the
applicable package/install and reopen evidence, before Human Gate 2 repeats.
