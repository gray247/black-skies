# Program 6 Batch P6-F Luna High Handoff Prompt

This is the bounded P6-F qualification handoff for `codex/foundation-audit`.
It follows durable P6-E commit `bb7f45db8f095cbab6d0780512db82e0c60f37c0` and
the newest explicit sequential Program 6 authorization.

## Scope

Produce complete mechanical evidence for the Program 6 story-intelligence
boundary using synthetic, non-protected projects only:

- deterministic Timeline, Continuity, and Emotion Graph lanes remain separate;
- the optional local-inference lane remains policy-gated, local-only,
  manually requested, temporary, advisory, and non-persistent;
- project isolation, project binding, stale/protected denial, offline failure,
  AI-disabled behavior, Focus suppression, keyboard/large-text/reduced-motion
  presentation, and currentness are evidenced;
- existing clean Stage 19 automated, Playwright/Electron, accessibility, and
  performance gates are rerun; and
- the three bounded review projects are represented without protected author
  material.

Do not add providers, model packages, dependencies, background work, generic
chat, durable memory, prose/outline/canon mutation, installed release claims,
or Program 7 behavior. Do not use protected author material as evidence.

## Required gates and stop rule

Run the focused P6-F qualification suite, full app typecheck, production build,
app lint, documentation lint, `git diff --check`, and complete dirty Stage 19
regression. Review the complete diff for unrelated files. After a green dirty
gate, update the receipt and authority records, stage only verified P6-F files,
commit the bounded evidence change, run the complete clean fixed gate, push
`origin/codex/foundation-audit`, and verify exact upstream equality and a clean
worktree. P6-G is only the author checkpoint and remains next.
