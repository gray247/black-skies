# Program 6 Batch P6-B Luna High Handoff Prompt

## Execution authorization record

This stored prompt records the explicit authorization for Program 6 Batch
P6-B. Storing this file, linking it, or referring to it does not authorize
another run.

- Model: `GPT-5.6 Luna`
- Reasoning effort: `high`
- Starting P6-A commit: `91a695044c25fb08b910774c5aa401e3bc8d8e71`
- Branch: `codex/foundation-audit`
- Expected upstream: `origin/codex/foundation-audit`
- Canonical checkout: `C:\Users\gray2\.codex\worktrees\4f0b\black-skies`
- Planning baseline: `235f42d7599aff5662eb1f65a0c0b201b4857e0c`

Jason explicitly authorized deterministic Emotion Graph V1 contracts and pure
projection logic, minimal backward-compatible P6-A extensions, persisted
author-entered planned/observed/optional-reader-effect records, temporary
inferred candidates, a bounded reusable accessible renderer, focused tests,
documentation, staging, one commit, and a normal push.

## Required boundary

P6-B must use the P6-A Story Intelligence document and repository, stable
`StoryPositionRefV1` values, flexible author labels, and qualitative intensity
only: `very-low`, `low`, `medium`, `high`, `very-high`, or `unknown`. Planned,
observed, inferred, and reader-effect lanes remain distinct. Inferred points
are deterministic, temporary, source-linked candidates and never enter
`story-intelligence.json`. The projection must expose ordering, qualitative
movement, planned-versus-observed comparison, stale/unavailable references,
default lane visibility, subject filtering, and an accessible text summary.

The component is reusable but must not be mounted in Writing Surface or Story
Knowledge production entry points during P6-B. No analyzer, prose heuristic,
model/provider call, universal taxonomy, score/percentage, diagnosis,
truth-owner mutation, new dependency, packaging, installer, Human Gate 4
claim, P6-C behavior, branch change, worktree action, amend, or force-push is
authorized.

## Required gates

Before mutation, verify the exact checkout, branch, clean state, synchronized
P6-A HEAD, planning-baseline ancestry, and the four retained worktrees. Stop on
any mismatch. Before commit, run the focused P6-A/P6-B Vitest suites,
renderer/shared typecheck, main compile, renderer production build, app lint,
documentation lint, `git diff --check`, and the fixed Stage 19 regression when
its changed boundaries are included. Review the complete diff and stage only
verified P6-B files.

Commit exactly:

```text
feat(program-6): add deterministic emotion graph
```

Push normally to `origin/codex/foundation-audit`, then confirm a clean
worktree and local `HEAD` equal to upstream. Stop after P6-B; P6-C is not
implicitly authorized.
