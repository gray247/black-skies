# Program 6 Batch P6-E Luna High Handoff Prompt

This is the bounded P6-E implementation handoff for `codex/foundation-audit`.
It follows the durable P6-D commit `32d0c61757126a1da4c812bfee0ace62759f5694`
and the newest explicit sequential Program 6 authorization.

## Scope

Implement only:

- a quiet Writing Surface intelligence cue;
- an existing Story Knowledge Command Center detail path over loaded project
  structure;
- Focus behavior that hides intelligence presentation without changing settings;
- a local-only, manually requested, temporary, advisory local-inference gateway
  contract, with loopback endpoint validation, policy permission checks, bounded
  response validation, timeout, and no persistence.

Preserve keyboard access, large-text reflow, reduced motion, non-color meaning,
unavailable/stale/protected handling, and the AI-disabled default. Story
Knowledge remains derived/supporting and does not own manuscript, outline,
assertion, lore, emotion, continuity, chronology, pacing, pressure, canon,
quality, or revision truth.

## Explicit exclusions

Do not add a provider, model package, dependency, network call, background
worker, provider router, generic chat path, durable memory, prose mutation,
outline mutation, settings mutation, startup/Electron change, packaging,
installer work, creation/revision workflow, P6-F qualification, P6-G author
review, Program 7, or Program 6 closure.

The gateway must reject disabled policy, protected/AI-excluded sources,
non-loopback endpoints, invalid responses, transport failure, and timeout. A
successful response is an inferred candidate only, with provenance, citation
requirement, expiry, and no durable write path.

## Required gates and stop rule

Run the focused P6-E gateway/component/surface tests, full app typecheck,
production build, app lint, documentation lint, `git diff --check`, and the
complete dirty Stage 19 regression. Stop on any failed gate, authority
mismatch, unrelated dirty file, truth-owner mutation, network/provider
boundary violation, or protection failure. After a green dirty gate, update
the receipt and current authority records, stage only verified P6-E files,
commit the bounded change, run the complete clean fixed gate, push
`origin/codex/foundation-audit`, and verify exact upstream equality and a clean
worktree. P6-F remains closed until P6-E is durable.
