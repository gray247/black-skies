# Program 6 Batch P6-C — Luna High Handoff

## Authorization and boundary

This record captures the original P6-C handoff executed with GPT-5.6 Luna at
high reasoning on 2026-09-01. Its deterministic Continuity V1 contracts, tests,
bounded review presentation, and evidence requirements remain controlling for
P6-C on `codex/foundation-audit`. A newer explicit user instruction recorded
in the current Program 6 authority records supersedes this record's older
batch-specific prohibition on later batches and authorizes sequential P6-D
through P6-G continuation after the preceding batch is complete. It does not
authorize Program 7 or Program 6 closure without final author acceptance.

Required starting identity was the clean, synchronized P6-B commit
`3f2dee97afbb9e7f7ca9f45c22cd9798bb562f61`, with P6-A baseline
`91a695044c25fb08b910774c5aa401e3bc8d8e71` retained in history. The canonical
checkout is the `4f0b` worktree on `codex/foundation-audit`.

## P6-C implementation contract

- Continuity V1 is a pure deterministic analyzer over explicit structured
  project, unit, fact, event, carryover, causal-dependency, and source-anchor
  records.
- Producers are limited to explicit POV mismatch, conflicting locked or
  accepted facts, invalid/unavailable/stale anchors, duplicate stable event
  identities or explicit duplicate references, author-defined carryover
  omission, and missing causal predecessors.
- Findings contain project/generation binding, deterministic producer identity,
  category, exact source/evidence references, scope, currentness, evidence
  class, confidence, impact, explanation, lineage, and bounded action
  descriptors.
- Unreliable narration, false belief, intentional ambiguity, disputed,
  hidden/unrevealed, red-herring, unknown, and intentional-divergence states
  are preserved and are not emitted as objective errors.
- Findings remain candidate-only. They do not mutate prose, assertions,
  notes, memory, signals, or author decisions. Recurrence retains lineage and
  does not reopen or resolve prior decisions. Stale evidence cannot be
  elevated directly to durable signal state.
- Protected and excluded source classes are blocked without raw-content
  leakage. Deterministic-only sources remain eligible for deterministic work.
- The reusable Continuity Review component is accessible and unmounted from
  production; no startup, Electron, dependency, or product-surface change is
  included.

## Required gates and stop rule

Run the focused P6-A/P6-B/P6-C suite, typecheck, main compile, renderer build,
app lint, documentation lint, diff check, and the complete Stage 19 regression
in the prescribed order. Stop on any failed gate, authority mismatch, product
failure, unrelated dirty file, or failed exact clean-commit synchronization.
The original execution and the one authorized unchanged Electron startup
retry both stopped at the same host preflight failure after the critical matrix
passed; no commit or push was made. The newer explicit user instruction
renews bounded diagnosis and repair authority for this P6-C continuation and
sequentially admits later batches only after their preceding batch is complete.
