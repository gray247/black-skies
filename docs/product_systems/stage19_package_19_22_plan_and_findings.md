# Stage 19 Package 19.22 Plan and Findings

## 1. Authority and outcome

Package `19.22` is active. It may close Stage 19 only as a bounded internal
engineering baseline:

```text
PACKAGE_19_22: ACTIVE
STAGE_19: OPEN
INTERNAL_V1_BASELINE: NOT_YET_COMPLETE
PUBLIC_RELEASE: NOT_AUTHORIZED
ALPHA_OR_BETA: NOT_CLAIMED
V3_PRODUCT_PROGRAM: NOT_STARTED_BY_THIS_PACKAGE
```

The accepted V1 product claim is limited to the two-window Windows writing
foundation, local project and manuscript-unit management, bounded editing,
durable Save/reopen/recovery, deterministic Markdown export, optional
explicitly approved advisory critique, and offline core operation.

Package `19.22` does not authorize a tag, public release, signing request,
publication, announcement, connector, provider qualification, V2/V3 feature,
or production-readiness claim.

## 2. Candidate invalidation rule

The accepted Package `19.20` installer remains historical qualification
evidence until Package `19.22` completes its candidate-impact review.

Changes to runtime source, manifests, lockfiles, dependency patches, resources,
version data, Electron Builder configuration, production-build scripts, or
packaging-verifier scripts invalidate the prior candidate for current closure
purposes and require rebuild plus affected qualification.

Documentation, tests, findings records, and CI-only changes do not enter the
binary unless artifact inspection proves otherwise. For Package `19.22`
closure, however, any tracked change before Jason's verdict supersedes the
candidate and requires a new exact-commit evidence set. Only the final
documentation-only closure commit may follow the qualified candidate.

## 3. Closed-loop protocol

Every finding follows:

```text
discover -> ledger -> reproduce -> root cause -> regression witness
-> minimal repair -> focused rerun -> subsystem rerun -> invalidated gates
-> clean full matrix -> exact-commit CI -> closure or reopen
```

No warning ceiling, suppression, new skip, timeout increase, retry increase,
or weakened assertion may substitute for repair.

## 4. Findings ledger

| ID | Severity | Subsystem / consequence | Reproduction and root cause | Candidate impact | Disposition / owner | Verification | Status / reopening trigger |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BS-19.22-P1-01 | P1 | First-party app lint can pass with six React Hook dependency warnings, allowing stale closures and hiding future warnings. | `pnpm --filter app lint`; five warnings in `App.tsx`, one in `ProjectHome.tsx`. The runner explicitly allowed six warnings and forced deprecated eslintrc mode. | Yes. Correct dependency handling affected renderer source and invalidated the prior candidate. | Package `19.22`: make dependencies complete, migrate the invoked lint path to flat config, and require zero warnings. | Flat-config app lint and Stage 19 lint report zero errors and zero warnings; focused renderer suites, full Vitest (`965` passed, `2` declared skips), strict typecheck, and production build pass. Full Stage 19, installed proof, and exact-commit CI remain. | LOCALLY_CLOSED_PENDING_OUTER_LOOP. Reopen on any lint warning, stale-closure regression, or config path that bypasses zero-warning enforcement. |
| BS-19.22-P1-02 | P1 | Clean Windows Stage 19 Electron matrix could not create its first window inside the Codex process sandbox and then hung during teardown. | Minimal Electron, production main, and Playwright all reproduced GPU child exit `-1073741515` inside the nested Codex sandbox on `39.8.9`, `41.10.3`, and `37.10.3`. The same minimal app and unmodified Playwright launch outside that sandbox pass on `39.8.9`; this proves an execution-environment boundary rather than a missing host DLL or Electron-line defect. | No retained dependency change. Manifest, lockfile, and installed dependency tree are restored to `39.8.9`; candidate invalidation comes from other runtime changes. | Package `19.22`: run Windows Electron qualification outside the nested sandbox, keep production launch unchanged, prohibit test-only GPU flags, and assert retry/teardown policy permanently. | Accessibility passed three focused runs; split-window activation passed three focused runs; complete inventory passed `63` with four reasoned opt-in/external-evidence skips, retries zero, and zero surviving Electron processes. Second full inventory, Stage 19, package, installed proof, and CI remain. | LOCALLY_CLOSED_PENDING_OUTER_LOOP. Reopen on any GPU child failure outside the nested sandbox, missing first window, leaked process, retry, or teardown timeout. |
| BS-19.22-P2-03 | P2 | Package verification did not explicitly reject credential-like filenames inside otherwise allowed production directories. | Static inspection of `findForbiddenPackagePaths`; `.env` was rejected, but `dist/credentials.json` and `dist/session-token.txt` were not. | Yes: packaging-verifier input changed, so current closure must rebuild and requalify the internal artifact. | Package `19.22`: reject credential/secret/token filename families and add a permanent verifier test. | Focused verifier regression and full Vitest pass. Package preflight/build, artifact inspection, installed proof, and CI remain. | LOCALLY_CLOSED_PENDING_ARTIFACT. Reopen if credential-like material can enter the ASAR or any legitimate packaged file is falsely rejected. |
| BS-19.22-P3-04 | P3 | A truthful export notice can remain visible after project switch. | Package `19.21` Computer Use rehearsal and provenance coverage showed that a completed or cancelled export could outlive its authoritative project generation. | Yes. Renderer source changes invalidate the prior candidate. | Package `19.22`: bind export publication to captured project ID and generation; clear notices on authoritative generation change; discard stale success, cancellation, and failure results. | Focused A-to-B, A-to-B-to-A, same-project revision, cancellation, and next-project export coverage must pass; installed proof remains. | OPEN_PENDING_EXACT_CANDIDATE. Reopen on any stale notice, wrong attribution, blocked next export, or obscured Save/export truth. |
| BS-19.22-P1-25 | P1 | A newly created Windows project can have a different lexical path identity from the canonical path returned on open, making valid Save/recovery bindings fail as stale. | Exact Windows Packaging Proof `30860268774`, fixed regression matrix, failed before package build: GitHub-hosted Windows supplied `C:\\Users\\RUNNER~1\\...` to bootstrap while `loadProjectForSpine` returned `C:\\Users\\runneradmin\\...` through `fs.realpath`. The mismatch cascaded into `STALE_SESSION` recovery/save assertions. | Yes. `projectBootstrap.ts` now returns `fs.realpath(projectPath)` after atomic rename; previous candidate `1080f9bd26f724f7cab6facc543e1824b2f817f8` and every run/artifact claim for it are historical. | Package `19.22`: preserve the existing canonical loader/open identity at bootstrap; do not weaken stale-binding checks. | Owning bootstrap realpath regression, project-spine IPC/repository regression, full Stage 19 regression, and exact new-candidate Windows Packaging Proof must pass without retries or new skips. | OPEN_PENDING_EXACT_CANDIDATE. Reopen on any lexical/canonical path split, `STALE_SESSION` for a current project, retry, warning, or teardown leak. |
| BS-19.22-P1-05 | P1 | The first zero-warning hook repair prevented `App` from rendering, collapsing 66 renderer tests before user interaction. | `pnpm --filter app test`; `ReferenceError: Cannot access 'draftEdits' before initialization` at dependency-array evaluation. Adding later-declared state to an earlier callback crossed JavaScript's temporal-dead-zone boundary. | Yes: renderer runtime source changed, so the previous internal artifact is historical evidence. | Package `19.22`: preserve fresh draft-edit access through the existing ref-backed synchronization pattern, without suppression or stale closure. | Focused App suites, complete Vitest, zero-warning lint, strict typecheck, and production build pass. Stage 19, installed proof, and CI remain. | LOCALLY_CLOSED_PENDING_OUTER_LOOP. Reopen if render initialization fails, cleared scene selection serializes stale edits, or exhaustive-deps reports a warning. |
| BS-19.22-P0-06 | P0 | The complete Vitest inventory could fail while its package command exited successfully, allowing every caller and CI gate to accept a red suite. | A 14-failure run returned `0`; `run-vitest-offline.mjs` read nonexistent `ctx.config.exitCode` and overwrote Vitest's authoritative failure code. | No packaged application input; test-gate runner only. | Package `19.22`: propagate Vitest's process exit code and add a permanent failing-fixture witness. | Permanent exit-code regression, focused red/green witnesses, and complete Vitest pass. Stage 19 callers and CI remain. | LOCALLY_CLOSED_PENDING_CI. Reopen if any failed, unhandled-error, or no-test Vitest run returns success. |
| BS-19.22-P1-07 | P1 | Forty-two tests in three full-inventory App suites exercised an identity-invalid project and never reached their asserted critique, preflight, and recovery behavior. | `AppCritique`, `AppPreflight`, and `AppRecovery` ordinary fixtures omitted `projectId`, while the accepted boundary correctly rejected them. The broken Vitest exit propagation concealed the gap. | No; tests only. | Package `19.22`: give ordinary fixtures stable synthetic identities while preserving dedicated rejection witnesses. | Three owning suites and complete Vitest pass; missing-identity witnesses still reject. Stage 19 and CI remain. | LOCALLY_CLOSED_PENDING_CI. Reopen if any non-identity test crosses activation without a project ID or a missing-identity witness stops rejecting. |
| BS-19.22-P1-08 | P1 | The required Python 3.11 lane was unavailable and the test runner hard-coded the unrelated `.venv`; long Windows pytest roots also produced misleading `FileNotFoundError` and snapshot permission failures. Ten tests also skipped ordinary deterministic work under a broad report-only environment label. | Install Python `3.11.9`, create `.venv311`, run `run_tests.ps1 -Group all -PythonPath .\.venv311\Scripts\python.exe -ReportSkips`, and reproduce restore tests under both the long user temp root and short `C:\tmp` root. The long root crossed legacy Windows path limits. One skip was an unfinished precedence placeholder and nine unnecessarily withheld supported deterministic checks. | No packaged application input. Test infrastructure, Python metadata, and first-party Python lint/type/test repairs only. | Package `19.22`: accept an explicit interpreter, use a short Windows temp root, keep generated paths outside lint scope, repair all first-party Black, Flake8, and Mypy findings, replace the placeholder with a deterministic precedence witness, and run supported replay/lock-mode checks on every host. | Black: 360 files clean. Flake8: zero findings. Mypy: 174 source files clean. The focused 19-test cluster passed three times. Exact all-group suite: 733 passed, zero skips, zero failures under Python 3.11. | LOCALLY_CLOSED_PENDING_CI. Reopen on any Python gate failure, skip, interpreter drift, nondeterministic replay, or test path that again crosses the Windows path boundary. |
| BS-19.22-P1-09 | P1 | An unbounded `docs/**/*.md` lint included inert archives while the old current lint checked only four hand-picked files. | The broad command reported 668 mixed historical/current diagnostics; neither command defined current authority. Current docs also improperly implied that Package `19.22` owned public-release authority. | No. | Package `19.22`: define a tracked current-authority inventory, name historical-root exclusions, lint and resolve links for every listed document, and reject stale public-release claims. | `pnpm lint:docs` verifies 21 current documents, resolves every local link, and rejects stale public-release claims. The handover and rollback policy now define only an internal baseline boundary. CI remains. | LOCALLY_CLOSED_PENDING_CI. Reopen on any inventory lint error, stale link, unnamed exclusion, or historical/public-release plan presented as current authority. |
| BS-19.22-P1-10 | P1 | The configured full renderer/shared/electron TypeScript no-emit project was red despite the narrow Stage 19 typecheck passing. | Full strict check initially emitted 394 lines across preload bridge completeness, project loading, fixtures, docking layouts, hooks, shared contracts, and renderer utilities. Root-cause clusters repaired production contracts first, normalized legacy input once, and moved tests to typed factories/current shapes. | Yes: production renderer, preload, and shared source changed. | Package `19.22`: require `app/tsconfig.json` through root/app `typecheck:all`, Stage 19 regression, and CI without exclusions or weaker strictness. | `pnpm typecheck:all` reports zero diagnostics; production build, full Vitest, app lint, and Stage 19 lint pass. Full Stage 19 and CI remain. | LOCALLY_CLOSED_PENDING_OUTER_LOOP. Reopen on any strict diagnostic or configuration change that removes first-party production, test, Electron, renderer, shared, or declaration inputs. |
| BS-19.22-P1-11 | P1 | Qualification and artifact-import suites timed out only in the complete parallel Vitest inventory. | The qualification suite performed independent setup sequentially, then the high-core host saturated filesystem work when Vitest ran its unbounded default worker inventory. Isolated tests passed, but the complete inventory remained order/contention sensitive. | No packaged runtime input; the Vite configuration change is limited to the test runner's `test` block. | Package `19.22`: parallelize independent qualification setup and bound Vitest to four workers in both owning configs. Preserve default test/hook timeouts, zero retries, and every assertion. | The three-suite owning cluster passed three consecutive runs (`255` passed, two declared opt-in skips each); complete Vitest passed `968` plus the same two declared skips. A permanent runner-policy test asserts `maxWorkers: 4` and prohibits timeout or retry inflation. Stage 19 and CI remain. | LOCALLY_CLOSED_PENDING_CI. Reopen on timeout, order dependence, retry-only pass, worker-policy drift, or qualification/artifact assertion drift. |
| BS-19.22-P1-12 | P1 | The installed dependency tree remained on diagnostic Electron `37.10.3` after manifest and lockfile were restored to `39.8.9`, so local launches did not prove the declared dependency. | Direct inspection of `app/node_modules/electron/package.json` disagreed with manifest/lock; a frozen reinstall restored the exact declared tree. | No tracked candidate input changed; the rebuilt candidate will use the restored lock. | Package `19.22`: verify manifest, lock, installed module, Windows version encoding, and installer identity together. | Installed module now reports `39.8.9`; all focused and full Electron evidence above used that exact version. Package and CI remain. | LOCALLY_CLOSED_PENDING_ARTIFACT. Reopen on any manifest/lock/module/artifact Electron-version mismatch. |
| BS-19.22-P1-13 | P1 | The shared Electron fixture allowed repository `runtime.yaml` to leak Stage 19 split-window mode into legacy tests, then the first repair exposed an obsolete hidden-shell smoke assertion. | Legacy specs expected the one-window dock app, but inherited `experimental_split_command_workspace: true`; the split smoke enabled the current two-window runtime while still waiting for legacy `app-root`. | No; tests and fixture infrastructure only. | Package `19.22`: make false and true fixture modes explicit in isolated temporary YAML, and update the smoke witness to assert the current dedicated Writing Studio/Command Center contract. | Legacy budget+dock owning suites pass; split activation passes three times; two consecutive complete Electron inventories each pass `63` plus four declared skips, retries zero, with zero leaked processes. The fixed Stage 19 matrix passes `19/19`. CI remains. | LOCALLY_CLOSED_PENDING_CI. Reopen on runtime-config leakage, wrong window mode, first-window timeout, retry, or surviving Electron descendant. |
| BS-19.22-P1-14 | P1 | The 100-unit Electron performance witness exceeded its 15-second creation ceiling during the second complete inventory. | The durable repository operation reparsed every existing draft after each structural append, producing quadratic I/O. A compiled repository-only diagnostic also separated repository work from Electron observation cost. Forced full Playwright tracing magnified 100 high-frequency renderer updates and is retained only as failure diagnostics, not as workload timing evidence. | Yes. Main-process project-spine runtime changed, so the prior candidate remains historical and the new candidate requires full requalification. | Package `19.22`: preserve exclusive draft creation and atomic outline commit, update the already-validated in-memory project incrementally when it still matches the durable outline, and fail over to a full reload on divergence. Keep the 15-second creation and 3-second selection ceilings unchanged. | Strict typecheck and production build pass; repository/Ipc owning suites pass (`31` tests). The rebuilt production bundle passed the focused zero-retry witness three times at `2011`–`2142` ms creation and `47`–`58` ms selection; consecutive full inventories passed at `1988/43` and `1893/50` ms; fixed Stage 19 passed at `2355/103` ms. Artifact, installed proof, and CI remain. | LOCALLY_CLOSED_PENDING_ARTIFACT. Reopen on either unchanged ceiling failing without forced trace instrumentation, durable reopen mismatch, disk/in-memory divergence mishandling, retry, or process leak. |
| BS-19.22-P0-15 | P0 | The authoritative truth lane inherited repository split-window configuration, then inherited ambient provider policy and credentials and attempted an unauthorized remote critique call. | The legacy dock truth harness expected `window.projectLoader`, but launched against the Stage 19 preload and crashed on an undefined payload. After runtime isolation, inherited `api_only` configuration selected the OpenAI adapter and attempted a request. | No packaged input; truth-runner and permanent test policy only. | Package `19.22`: generate an isolated dock-mode runtime config, scrub every supported OpenAI credential variable from the backend child, force `provider_calls_enabled=0`, and retain only deterministic local route metadata with adapter execution disabled. Add the policy witness to Stage 19 regression. | The policy witness passed three consecutive runs. The Python 3.11 truth lane completed its UI/service/accept/snapshot/recovery/export chain and wrote valid receipts with fallback provenance and `provider_called=false`. The full gauntlet later passed truth again. CI remains. | LOCALLY_CLOSED_PENDING_CI. Reopen if truth consumes repository runtime mode, exposes a retired preload surface, inherits any provider credential/policy, reports `provider_called=true`, or launches a local/remote model adapter. |
| BS-19.22-P1-16 | P1 | Service-truth and gauntlet interpreter/temp contracts could silently leave Python 3.11 or fail before collection under repository ACLs. | Both scripts preferred `.venv`; service truth also hard-coded a repository `codex_temp` root that reproduced `PermissionError` before pytest. | No packaged input; Python verification infrastructure only. | Package `19.22`: honor explicit `PYTHON`, prefer `.venv311`, use the established `BLACKSKIES_TEST_TEMP_ROOT`/short Windows temp boundary, and retain policy tests in Stage 19 regression. | Python source compiles; policy tests pass; service truth passes `19/19` under Python 3.11 at `C:\tmp`; full gauntlet uses the same interpreter and passes. Full Python/static rerun and CI remain. | LOCALLY_CLOSED_PENDING_OUTER_LOOP. Reopen on interpreter drift, repository-temp ACL failure, collection failure, or any lane that silently falls back away from configured Python 3.11. |
| BS-19.22-P1-17 | P1 | Gauntlet PASS 3 always selected zero tests because app-workspace Vitest received repository-relative paths prefixed with `app/`. | The captured PASS 3 log reported `No test files found`; all other gauntlet passes were green. | No packaged input; gauntlet command and policy test only. | Package `19.22`: pass app-workspace-relative test paths and prohibit the stale prefix in a permanent policy witness. | The exact three-suite cluster passed three consecutive runs (`31/31` each); the complete six-pass gauntlet then returned `MERGE` with high confidence and no blocking issue. CI remains. | LOCALLY_CLOSED_PENDING_CI. Reopen on zero-test selection, any PASS 3 failure, or gauntlet verdict below high-confidence `MERGE`. |
| BS-19.22-P1-18 | P1 | Seven Vitest suites passed only when invoked from the app workspace; the root direct inventory failed before exercising their contracts. | The root runner exposed repository-path construction from `process.cwd()`, even though the official app runner selects `app` as its working directory. That made test and policy inputs depend on the caller rather than the owning file. | No packaged input; tests only. | Package `19.22`: anchor repository and app fixtures to each test module through `import.meta.dirname`; preserve both supported invocation boundaries. | The exact seven-suite cluster passed three consecutive runs (`40/40` each), and the official complete inventory passed `974` with two declared opt-in skips. CI remains. | LOCALLY_CLOSED_PENDING_CI. Reopen if any test fixture or repository-policy path changes meaning with the supported runner's working directory. |
| BS-19.22-P1-19 | P1 | The complete Vitest inventory passed while emitting React updates-outside-`act` warnings, leaving asynchronous assertions and remount behavior potentially order-dependent. | Offline analytics asserted before its effect committed; the insights pane created nested render roots from an event callback; project recovery witnesses stopped before the complete fallback sequence; service-health witnesses accepted their initial offline value before the health effect settled; and the Stage 19 recovery witness split one logical external update across separate act boundaries. Exact-commit Linux run `30569031837`, job `90960727603`, named the remaining warning-producing cases even though every assertion passed. | No packaged input; tests only. | Package `19.22`: model analytics opening as one React tree; await complete service, recovery, and analytics effects; keep related external Stage 19 updates in one act boundary; make asynchronous witnesses assert final authority rather than an intermediate value. Do not suppress console output, nest test render roots, or weaken assertions. | The focused owning cluster passes (`98/98`) with zero warnings locally; app lint and full strict TypeScript pass. Exact-commit Linux confirmation remains. | LOCALLY_CLOSED_PENDING_CI. Reopen on any React `act` warning, assertion-before-commit behavior, nested test render root, or witness that can pass before the owning asynchronous operation completes. |
| BS-19.22-P1-20 | P1 | The first exact-commit candidate built successfully but package verification rejected legitimate third-party JavaScript modules as credential material. | `pnpm --dir app run package:win` rejected Lezer `tokens.js`, `js-tokens`, and React `ReactPropTypesSecret.js`. The credential basename rule matched keyword substrings without distinguishing executable source from credential-like data/key files. | Yes. The packaging verifier is candidate-affecting under the Package 19.22 boundary, so the verifier repair requires a new exact commit and candidate qualification even though the first artifact bytes were valid. | Package `19.22`: retain `.env` rejection; restrict credential-name matching to delimited credential terms with data, certificate, or key extensions; add the exact third-party false positives as acceptance witnesses. | The focused verifier suite passes (`3/3`). The repaired verifier accepts the built ASAR with `2,571` entries, `2,334` integrity records, and zero forbidden paths while reporting both executable and installer `NotSigned`. New exact-commit rebuild and CI remain. | LOCALLY_CLOSED_PENDING_NEW_CANDIDATE. Reopen if a credential-like data/key file passes, an ordinary dependency source file is rejected, or the exact candidate reports any forbidden path. |
| BS-19.22-P1-21 | P1 | The exact-commit Linux full unit inventory failed one recovery-context assertion although Windows passed. | CI run `30567954794`, job `90957091052`: the fixture supplied a Windows-form path on Linux, while the coordinator correctly returned its canonical `path.resolve` identity. The assertion compared canonical output with unnormalized fixture input. | No packaged input; test expectation only. | Package `19.22`: assert the coordinator's cross-platform canonical path contract explicitly without changing runtime normalization. | The focused coordinator suite passes (`12/12`) and app lint is clean. Exact-commit CI rerun remains. | LOCALLY_CLOSED_PENDING_CI. Reopen on any platform-dependent identity assertion or mismatch between binding validation and returned recovery context. |
| BS-19.22-P1-22 | P1 | The exact-commit Linux Playwright inventory observed enabled actions during the transition from no-project startup to automatic sample-project rehydration. | CI run `30568418898`, job `90958648413`: canary launch, fixture preparation, and `62` other tests passed; `startup_authority_contract` sampled the project-loaded marker once before the authority state settled, then asserted the earlier no-project branch against buttons already enabled by completed rehydration. | No packaged input; Electron contract test only. | Package `19.22`: use the suite's existing bounded authority-settlement helper before classifying the initial state, so the witness accepts either a stably disabled no-project state or completed project rehydration, never a mixed transition. | The complete focused startup-authority Electron suite passes `11/11` outside the nested sandbox with zero retries. Exact-commit Linux CI remains. | LOCALLY_CLOSED_PENDING_CI. Reopen on any mixed startup classification, falsely enabled no-project action, or failure to enable actions after authoritative project/scene activation. |
| BS-19.22-P1-23 | P1 | The old ledger identified `a88ed9b` as current and implied a final Jason judgment, while current authority remained open. | Exact Linux runs `30571155600` and `30571211360` passed at `0d4e05d`; neither is Windows artifact, installed-lifecycle, nor final-judgment evidence. The P3 renderer repair supersedes both commits for closure. | Yes. The renderer repair requires a new package and exact-commit evidence. | Package `19.22`: record all earlier runs as historical, qualify one new exact candidate, and obtain a verdict naming that full SHA. | Candidate receipt, Windows artifact/installed proof, exact CI, and final closure receipt must agree. | OPEN_PENDING_EXACT_CANDIDATE. |
| BS-19.22-P3-24 | P3 | The prior notice-lifetime disposition named no approved future resolution stage. | The prior `Post-V1/V3` owner was undefined while the V3 program was not authorized. | Yes. This package resolves the limitation in runtime instead of deferring it. | Package `19.22`: close through the P3-04 repair and its regression coverage. | Same evidence as P3-04. | OPEN_PENDING_EXACT_CANDIDATE. |

## 5. Exact candidate and installed evidence

- Historical candidate commit: `a88ed9b62b429e0a11bbad54ad376815ffc98ad2`.
- Installer: `BlackSkies-Setup-1.0.0-rc1.exe`, `89,277,460` bytes,
  SHA-256 `fd0cd729f0acc3783f81161c4ad9646853a96b705f0c5ab901ab7f4e2c54719e`,
  `NotSigned`, built with publication disabled.
- ASAR: `15,083,810` bytes, SHA-256
  `75a3526860b70d3bc0f3dc8ad4ec13e2da91b92a54eed81c4064392a10e9d11b`,
  `2,571` entries, `2,334` integrity records, and zero forbidden paths.
- Installed smoke: packaged identity, two sandboxed windows, no forbidden runtime
  descendants, no core credentials or development flags, durable close/reopen,
  and exact deterministic Markdown export passed.
- Installed representative proof: `100` units created in `2,690.3372` ms,
  selected in `11.3134` ms, reopened clean, and exported with exact bytes.
- Approved uninstall exited `0`; the executable and uninstaller were removed.
  All `115` recorded external synthetic project/export files remained present
  with unchanged SHA-256 values. Reinstall exited `0` and restored the exact
  `210,950,656`-byte unsigned executable.
- The candidate evidence above is historical only. It cannot close Package
  `19.22` or substitute for a new exact candidate, exact Windows artifact,
  installed evidence, or a traceable final Jason judgment.

## 6. Retained boundary dispositions

- Package `19.14` numeric scoring, packet finalization, and another provider
  capture are retained in place as inactive historical qualification tooling.
  They are unauthorized and not current V1 authority.
- Package `19.14` output-quality limitations remain limitations of an optional
  advisory route, not proof of broad editorial intelligence.
- Package `19.18` monitor displacement remains bounded only while both windows
  stay reachable and ordinary writing remains possible.
- Historical lint, type, listener, editor, packaging, and dependency findings
  must resolve to a verified later repair or an exact owner and reopening
  trigger before Package `19.22` closes.

## 7. Stop conditions

Package `19.22` cannot close while any active first-party lint warning, test
failure, unexpected skip, flaky retry dependency, leaked process, build
failure, CI failure, unowned P2/P3, candidate-identity ambiguity, or
contradictory public-release language remains.

## 8. Execution authorization and candidate discipline

The governing authority has authorized bounded Package `19.22`
qualification work. Public release actions remain prohibited.

The final candidate is the single pushed commit containing every runtime,
test, qualification-helper, tracker, and pre-closure authority change. No
tracked change may follow it before Jason's verdict. The only allowed later
commit is a documentation-and-tracker-only closure receipt after an exact
`PASS` verdict naming that candidate SHA.
