# Stage 17 Vertical Slice Scope

## 1. Repo gate result

Repository checkpoint commands reviewed:

- `git rev-parse HEAD`
- `git status -sb`
- `git status --short`
- `git log -8 --oneline`

Gate result: pass.

- `HEAD`: `c2cd803137944e3e236a7ee39d1d9940874e8fd2`
- branch: `salvage/minimal-two-surface-shell`
- upstream state: synchronized with `origin/salvage/minimal-two-surface-shell`
- worktree: clean

Required recent history present:

- `docs(product): decide Stage 17 deferred issue slice impact`
- `docs(product): open Stage 17 vertical slice entry review`

## 2. Records inspected

The following records were inspected:

- `docs/product_systems/stage17_vertical_slice_entry_review.md`
- `docs/product_systems/stage17_deferred_issue_slice_impact_decisions.md`
- `docs/product_systems/stage17_entry_deferred_issue_obligation.md`
- `docs/product_systems/current_truth_index.md`
- `docs/product_systems/current_product_roadmap.md`
- `docs/product_systems/pre_code_discovery_plan.md`

## 3. Slice definition

The first vertical slice is:

A minimal buildable spine proving Black Skies can open a project, preserve project truth/authority, expose the two-surface workflow, and support one narrow writer-facing flow without broad feature expansion.

This scope record is planning authority only.

It does not:

- authorize coding
- authorize runtime changes
- authorize test work
- authorize witness creation or regeneration
- authorize cleanup/archive execution
- authorize protected-evidence mutation

## 4. Included systems with rationale

Included in first slice:

1. `project open/load authority`
   Rationale: the slice must prove Black Skies can open a project at all.
2. `project truth/identity display`
   Rationale: the slice must prove project truth and authority remain visible and governed.
3. `Writing Surface`
   Rationale: current doctrine makes Writing Surface sovereign and the slice must expose the primary authoring surface.
4. `Command Center`
   Rationale: the slice must expose the two-surface workflow without letting Command Center gate writing.
5. `one narrow manuscript/prose writing surface`
   Rationale: the slice must support one narrow writer-facing flow instead of broad feature spread.
6. `one narrow save-state/status flow visible to the writer`
   Rationale: the slice must show the writer a minimal honest status path without expanding into full degraded/recovery doctrine resolution.
7. `minimal project authority/status messaging`
   Rationale: the slice must make project state and authority understandable at a minimal usable level.

## 5. Excluded systems with rationale

Excluded from first slice:

1. `restore/import behavior`
   Rationale: Stage 17 slice-impact decisions excluded restore/import identity from the first slice.
2. `sample-root/protected-evidence path dependence`
   Rationale: the first slice must not depend on protected evidence or retained sample-root material.
3. `External Challenge / Current Validation substance`
   Rationale: full current-validation review belongs to Stage 18 unless Stage 17 finds direct slice impact.
4. `broad AI generation`
   Rationale: the first slice is a narrow project-open/truth/two-surface/writer-flow proof, not an AI-generation rollout.
5. `critique system`
   Rationale: critique is broader workflow substance and not necessary to prove the minimal slice spine.
6. `rewrite loop`
   Rationale: rewrite breadth would expand beyond the minimal first-slice writer flow.
7. `export/import pipeline`
   Rationale: the first slice must not require document interchange breadth or restore/import dependence.
8. `advanced diagnostics`
   Rationale: only minimal authority/status messaging is needed in the first slice.
9. `connector work`
   Rationale: connectors remain gated and are not required for the first slice.
10. `cleanup/archive execution`
    Rationale: Stage 16 is closed scope-only and Stage 17 is not a cleanup lane.
11. `snapshot/witness regeneration`
    Rationale: witnesses remain out of scope and are not authorized.
12. `broad test-suite stabilization`
    Rationale: Stage 17 defines scope; it does not authorize broad test recovery.
13. `packaging polish`
    Rationale: packaging breadth is outside the minimal slice proof.
14. `theming/polish beyond what is needed to see the two surfaces`
    Rationale: visual completeness beyond minimal two-surface visibility is unnecessary scope expansion.

## 6. Visible-but-non-mutating systems

Visible but non-mutating:

1. `project truth/authority indicators`
   Rationale: truth and authority must be visible, but the slice does not require broad truth-mutation workflows.
2. `save-state/status messaging`
   Rationale: the writer must see status, but the slice does not require full recovery or degraded-writing resolution.
3. `Command Center support context`
   Rationale: Command Center must be present in the two-surface shell, but it must not silently mutate project truth or gate writing.

## 7. Stage 18 promoted obligations

Stage 18 promoted obligations:

1. confirm restore/import identity exclusion still holds
2. perform full `External Challenge / Current Validation` review
3. confirm sample-root/protected-evidence exclusion still holds
4. receive any traceability concern that later proves readiness-relevant

## 8. Later-lane items with explicit non-impact statement

Later-lane items with explicit non-impact statement:

1. `later explicitly authorized Restore/Import Identity Validation lane`
   Non-impact statement: the first slice does not require restore/import behavior.
2. `later explicitly authorized External Challenge Follow-Up / Current Validation lane`
   Non-impact statement: the first slice does not require full external/current validation substance at Stage 17.
3. `later explicitly authorized current-versus-historical classification follow-up lane`
   Non-impact statement: the first slice does not require protected-evidence or retained sample-root path dependence.
4. broader critique, rewrite, AI-generation, connector, export/import, and packaging lanes
   Non-impact statement: these lanes are broader than the minimal first-slice proof and are not required to define the first slice honestly.

## 9. Blocking items

Blocking items:

- none, if the slice remains inside the boundaries defined in this record

The following become blocking if the slice is expanded to require them:

1. restore/import behavior
2. sample-root or protected-evidence path dependence
3. unresolved external/current validation substance
4. an unsplit unsafe-to-classify traceability concern that becomes slice-relevant
5. any included item that would silently require coding, tests, witnesses, cleanup/archive execution, or protected-evidence mutation to define the slice honestly

## 10. Protected evidence boundary

Protected evidence remains off-limits:

- `sample_project/proj_esther_estate/**`
- `sample_project/Esther_Estate/**`
- `build/truth_receipts/**`
- `build/runtime_truth.json`
- `build/runtime_truth.schema.json`
- `ci_artifacts/**`
- tracked snapshots
- IPC snapshot evidence
- real user projects

The first slice must not depend on protected evidence.

Nothing in this scope authorizes modification, regeneration, movement, rename, deletion, archive creation, cleanup, normalization, or casual test use of protected evidence.

## 11. Truth/authority boundary

Truth/authority boundary:

- project truth and authority must remain visible in the first slice
- no included system may silently mutate accepted truth
- project-opening and project-identity visibility must preserve doctrine rather than invent new truth owners
- the first slice proves governed authority visibility, not broad truth-mutation breadth

## 12. Two-surface workflow boundary

Two-surface workflow boundary:

- the first slice includes `Writing Surface` and `Command Center`
- `Writing Surface` remains sovereign
- `Command Center` supports writing and does not gate it
- the first slice proves the shell and relationship between the two surfaces, not broad cross-surface feature completion

## 13. Writer-facing flow boundary

Writer-facing flow boundary:

- one narrow manuscript/prose writing flow only
- one narrow save-state/status flow visible to the writer
- minimal authority/status messaging only
- no broad critique, rewrite, import/export, diagnostics, AI-generation, or polish expansion

## 14. Stage 19 implementation guardrails

Stage 19 implementation guardrails:

1. Stage 17 scope does not authorize coding
2. Stage 18 must confirm carried exclusions and promoted obligations before implementation readiness can pass
3. implementation remains blocked if restore/import identity becomes slice-relevant and unresolved
4. implementation remains blocked if required `External Challenge / Current Validation` review remains unresolved at Stage 18 closure
5. implementation remains blocked if sample-root/protected-evidence exclusion fails
6. implementation remains blocked if a traceability concern later proves readiness-relevant and unresolved

## 15. Recommended next safe action

Recommended next safe action:

- Jason review of this Stage 17 vertical slice scope record

After review, the next safe move is a read-only Stage 17 slice-spine definition pass that names the exact minimal project-open, truth/identity display, two-surface shell, narrow writing flow, and narrow status messaging spine without authorizing coding.

PZ_CONTINUE: Stage 17 vertical slice scope ready for Jason review
