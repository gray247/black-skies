# DEV STARTUP — Black Skies

Dev-only helpers for starting the backend and diagnosing startup issues.

## Install dev dependencies
From repo root:
```powershell
py -3.11 -m pip install -r services/requirements-dev.txt
