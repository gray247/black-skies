{
  "name": "black-skies",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "install": "pnpm install",
    "lint": "pnpm -r lint",
    "dev": "concurrently \"pnpm --filter app dev\" \"pnpm run electron:dev\"",
    "electron:dev": "echo \"Launching Electron shell (placeholder)\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "packageManager": "pnpm@8.15.4"
}
