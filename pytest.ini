[pytest]
testpaths =
    tests
    services/tests/unit/test_runtime_truth.py
addopts = -q -x --maxfail=1
markers =
    unit: Unit tests safe for fast offline execution.
    eval: Offline evaluation harness integration tests.
    contract: Contract-level checks validating external service expectations.
