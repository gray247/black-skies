# Compatibility entry point. The repository-root lockfiles are the only
# dependency authority; do not add unpinned service-local requirements here.
-r ../requirements.lock
-r ../requirements.dev.lock
