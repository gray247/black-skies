"""M1 placeholder for advisory-only non-mutation invariants."""

from __future__ import annotations

import pytest


@pytest.mark.skip(reason="M1 scaffold only; invariant test implementation pending.")
def test_memory_non_mutation_invariant() -> None:
    assert True

