# C6-5 — Create New Playwright E2E Test:
# gui.analytics_no_network_offline.spec.ts

## Goal
Ensure analytics NEVER call backend endpoints when offline:
- /analytics/summary NOT called
- /analytics/scenes NOT called
- Insights + Corkboard read from cache
- Offline banner appears
- Cached metrics still render (readability, pacing, dialogue)
- No crashes

## Read Before Coding
- app/tests/e2e/gui.insights.spec.ts
- app/tests/e2e/utils/*
- useLocalAnalyticsCache
- useServiceHealth
- AnalyticsDashboard
- docs/phase6_analytics_refinement_notes.md

## Instructions

### 1. Create file:
app/tests/e2e/gui.analytics_no_network_offline.spec.ts

### 2. Bootstrap:
- window.__APP_READY__
- load sample_project/Esther_Estate
- wait for dock workspace

### 3. Prime cache (online):
- Ensure service is online (overrideServices)
- Open Insights
- Wait for:
  - readability badge
  - pacing badge/strip
  - dialogue/narration ratio

### 4. Switch offline:
Use override:
```js
window.__dev.overrideServices({
  checkHealth() { throw new Error("offline"); },
  analytics: {
    summary() { throw new Error("should not be called"); },
    scenes() { throw new Error("should not be called"); }
  }
});
document.dispatchEvent(new CustomEvent("test:service-health"));
